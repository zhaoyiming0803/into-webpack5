{
  "name": "test-webpack5",
  "version": "0.0.1-alpha.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:cache": "rm -rf dist && webpack --config ./webpack.cache.config.js",
    "build:entry": "rm -rf dist && webpack --config ./webpack.config.entry.js",
    "build:output": "rm -rf dist && webpack --config ./webpack.config.output.js",
    "watch": "rm -rf dist && webpack --config ./webpack.config.js --watch",
    "build": "rm -rf dist && webpack --config ./webpack.config.js"
  },
  "license": "ISC",
  "devDependencies": {
    "acorn-walk": "^8.2.0",
    "chalk": "^2.3.2",
    "loader-utils": "^3.2.1",
    "rimraf": "^2.6.2",
    "schema-utils": "^4.2.0",
    "terser-webpack-plugin": "^5.3.9",
    "webpack": "^5.72.0",
    "webpack-cli": "^4.9.2",
    "webpack-merge": "^5.8.0"
  }
}
